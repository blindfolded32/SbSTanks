using System.Collections.Generic;
using Interfaces;
using UnityEngine;

namespace Shell
{
    public class ShellControllerData
    {
    /*    public List<Shell> shells = new List<Shell>(8);
        public readonly LayerMask shellMask = 6;
        public IUnit player;
        public IUnit enemy;
        public LayerMask groundMask;

        public const string PREFAB_PATH = "Prefabs/Shell";
        public const int SHELLS_COUNT = 5;
        public const float NEW_SHELL_OFFSET = 0.5f;
        public const float X_ROTATE_IN_FLY = 0.2f;

        public ShellControllerData(IUnit player, IUnit enemy)
        {
            this.player = player;
            this.enemy = enemy;
            groundMask = 1 << 8;
        }*/
    }
}